一、注意边界问题 特殊输入 错误处理
二、int 的范围 (-2^31 ~ 2^31-1)

1.将一个数组插入到另一个时，可以尝试使用从后面插入。
2.有序数组在遍历时可以采用对撞指针的方式，头和尾相向遍历。
3.两个有序的数组可以采用双指针策略。
4.c++的集合中不存在的键他的值为零。 有的语言直接不存在。


链表
1. 建立虚拟头节点 注意保留头节点和虚拟头节点的指针位置。
2.访问某个节点时要先确保其不为空。 防止越界问题
3.建立虚拟节点的时机 当链表进行删除操作时 要设立虚拟节点 因为删除节点要知道其上一个节点  如果待删除节点是头节点的话就无法工作 所以设立虚拟头节点


